{
  "name": "role-reversal-shooter",
  "version": "0.1.0",
  "description": "Role-reversal shooter: player controls enemies, AI hero learns and evolves",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  }
}
